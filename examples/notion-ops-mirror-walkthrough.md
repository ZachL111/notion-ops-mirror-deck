# Notion Ops Mirror Deck Walkthrough

This walk-through keeps the domain vocabulary close to the data instead of burying it in prose.

| Case | Focus | Score | Lane |
| --- | --- | ---: | --- |
| baseline | dry-run spread | 251 | ship |
| stress | rename risk | 221 | ship |
| edge | operator cost | 175 | ship |
| recovery | idempotence | 242 | ship |
| stale | dry-run spread | 197 | ship |

Start with `baseline` and `edge`. They create the widest contrast in this repository's fixture set, which makes them better review anchors than the middle cases.

The useful comparison is `dry-run spread` against `operator cost`, not the raw score alone.
